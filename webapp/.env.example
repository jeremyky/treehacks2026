# Command center API base URL. When using Vite dev server proxy (default), leave unset to use /api.
# When running without proxy (e.g. production or different ports), set to your command center URL:
# VITE_COMMAND_CENTER_URL=http://localhost:8000
